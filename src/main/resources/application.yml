spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/micuota
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080
jwt:
  secret: ChangeThisSecretKeyForJWTSigning
  expiration: 86400000
